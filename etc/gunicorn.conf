CONFIG = {
    'mode': 'wsgi', 				# протокол
    'working_dir': '/home/box/web', # папка, в которой находится обработчик hello.py
    'python': '/usr/bin/python', 	# папка интерпретатора питона
    'args': (
        '--bind=0.0.0.0:8080', 		# привязка сокета сервера
        '--workers=16',				# количество процессов обработки
        '--timeout=60',				# процессы, молчащие 60 секунд, убиваются и перезапускаются
        'hello',					# имя модуля обработчика
    ),
}
